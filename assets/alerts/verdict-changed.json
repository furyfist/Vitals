{
  "alert": "Vitals: verdict changed",
  "alertType": "METRIC_BASED_ALERT",
  "description": "$service.name $service.version — VERDICT CHANGED. Sigma, cause, and exemplars are on the trace-linked vitals verdict log (Logs -> filter service.name=vitals, service.version=$service.version) — not carried on this metric alert.",
  "ruleType": "threshold_rule",
  "evalWindow": "1m0s",
  "frequency": "1m0s",
  "condition": {
    "compositeQuery": {
      "queryType": "builder",
      "panelType": "graph",
      "queries": [
        {
          "type": "builder_query",
          "spec": {
            "name": "A",
            "signal": "metrics",
            "disabled": false,
            "aggregations": [
              {
                "metricName": "vitals.verdict.state",
                "timeAggregation": "max",
                "spaceAggregation": "max"
              }
            ],
            "groupBy": [
              { "name": "service.name" },
              { "name": "service.version" }
            ],
            "stepInterval": 60
          }
        }
      ]
    },
    "op": ">=",
    "target": 2,
    "matchType": "1",
    "targetUnit": ""
  },
  "labels": {
    "severity": "warning",
    "source": "vitals"
  },
  "annotations": {
    "description": "$service.name $service.version — VERDICT CHANGED. Sigma, cause, and exemplars are on the trace-linked vitals verdict log (Logs -> filter service.name=vitals, service.version=$service.version) — not carried on this metric alert.",
    "summary": "$service.name $service.version — VERDICT CHANGED"
  }
}
